"""Reference implementation of the flexible scraper described in ARCHITECTURE.md.

This package is illustrative. Imports of optional third-party libraries
(`curl_cffi`, `playwright`, `requests_ip_rotator`, etc.) are lazy so the
module surface stays importable without the full dependency set installed.
"""
